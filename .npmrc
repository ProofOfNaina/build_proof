# @shelby-protocol/react@1.0.3 pins a peer of @shelby-protocol/sdk@0.2.3, but the
# app depends on 0.2.4 (which is what the lockfile and the working tree install).
# Without this, a clean `npm install` fails on ERESOLVE. Remove once the Shelby
# packages agree on a version.
legacy-peer-deps=true
